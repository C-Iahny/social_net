# ─────────────────────────────────────────────────────────────────────────────
# ZOOT Social – dépendances de production
# ─────────────────────────────────────────────────────────────────────────────
# Le fichier requirements-dev.txt contient toutes les dépendances de
# développement (Jupyter, OpenCV, etc.) — ne pas utiliser en production.

# ── Django core ──────────────────────────────────────────────────────────────
Django==4.2.25
asgiref==3.7.2
sqlparse==0.5.3
pytz==2022.7.1

# ── ASGI / WebSockets (Channels + Daphne) ────────────────────────────────────
channels==4.0.0
channels-redis==4.2.0
daphne==4.0.0
redis==5.0.1
msgpack==1.0.7
async-timeout==4.0.3

# ── Serveur WSGI (fallback / Health checks) ──────────────────────────────────
gunicorn==20.1.0

# ── Base de données ──────────────────────────────────────────────────────────
# psycopg2-binary : version binaire pré-compilée, aucune dépendance système
psycopg2-binary==2.9.9
dj-database-url==1.0.0

# ── Configuration / variables d'environnement ────────────────────────────────
python-decouple==3.6

# ── Fichiers statiques ───────────────────────────────────────────────────────
whitenoise==6.2.0

# ── Images ───────────────────────────────────────────────────────────────────
Pillow==11.3.0
pillow-heif>=0.15.0    # Support HEIC/HEIF (iPhone) — s'enregistre comme plugin PIL

# ── Stockage cloud (médias persistants en production) ────────────────────────
boto3>=1.34.0
django-storages[s3]>=1.14.0

# ── Frontend / formulaires ───────────────────────────────────────────────────
django-ckeditor==6.5.1
django-js-asset==2.0.0
django-crispy-forms==2.1
crispy-bootstrap4==2024.1    # Template pack Bootstrap 4 requis par django-crispy-forms 2.x

# ── Interface d'administration ───────────────────────────────────────────────
django-jazzmin==2.6.0

# ── Divers Django ────────────────────────────────────────────────────────────
django-extensions==3.2.1

# ── Cryptographie / sécurité ─────────────────────────────────────────────────
cryptography==46.0.7
pyOpenSSL==25.3.0    # 25.1.0 exige cryptography<46 → conflit avec le pin ci-dessus

# ── Web Push Notifications (VAPID) ───────────────────────────────────────────
pywebpush==1.14.0

# ── Twisted (requis par Daphne/Channels) ─────────────────────────────────────
Twisted==23.10.0
autobahn==23.6.2
txaio==23.1.1
constantly==23.10.4
incremental==22.10.0
hyperlink==21.0.0
automat==22.10.0
zope.interface==6.1
service-identity==24.1.0
pyasn1==0.5.1
pyasn1-modules==0.3.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
requests==2.32.5
# La production tourne en Python 3.11 (runtime.txt) et reçoit urllib3 2.x,
# entièrement corrigé. Sur Python 3.9 (poste de dev), botocore impose
# urllib3<1.27 : on y installe la dernière 1.26.x maintenue, sinon
# `pip install -r requirements.txt` échoue en local.
urllib3==2.5.0 ; python_version >= "3.10"
urllib3==1.26.20 ; python_version < "3.10"
certifi==2025.8.3
idna==3.10
charset-normalizer==3.4.3

# ── Email transactionnel (HTTP API — SMTP bloqué par Railway) ────────────────
django-anymail[brevo]>=10.0

# ── SMS transactionnel (vérification téléphone, support Madagascar) ───────────
africastalking>=1.2.5

# ── Planificateur de tâches (cron digest push) ───────────────────────────────
APScheduler==3.10.4
bleach==6.0.0    # Assainissement du HTML utilisateur (filtre |safe_html) — anti-XSS stocké
